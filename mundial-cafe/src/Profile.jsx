import {Download,Upload,UserRound} from 'lucide-react';
export function Profile({profile,onSave,onExport,onImport}) {
  function submit(e){e.preventDefault();const f=new FormData(e.currentTarget);const tasters=[f.get('one').trim(),f.get('two').trim()];if(tasters.every(Boolean))onSave({tasters,city:f.get('city').trim()});}
  return <section className="profile-layout"><div className="panel profile-panel"><UserRound size={28} strokeWidth={1.3}/><h2>Participantes</h2><form onSubmit={submit}><div className="two-fields">{profile.tasters.map((t,i)=><label key={i}>Participante {i+1}<input name={i===0?'one':'two'} required maxLength={30} defaultValue={t}/></label>)}</div><label>Ciudad habitual<input name="city" maxLength={80} defaultValue={profile.city}/></label><p className="hint">Estos nombres se usan en torneos y visitas nuevos. Los que ya guardaron conservan sus participantes.</p><button className="primary">Guardar perfil</button></form></div><div className="panel backup-panel"><h2>Datos y respaldos</h2><p>Los torneos y las visitas se guardan en este navegador. No hay cuenta ni sincronización entre dispositivos.</p><p>Exportá una copia de tus datos para conservarlos o importarlos en otro navegador.</p><div className="backup-actions"><button onClick={onExport}><Download size={18}/>Exportar todo</button><button onClick={onImport}><Upload size={18}/>Importar respaldo</button></div><small>También podés importar el respaldo del primer mundial de café: se agrega como un torneo más.</small></div></section>;
}

